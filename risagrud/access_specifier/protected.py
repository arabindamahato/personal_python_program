# protected

_address = 'Bangalore'

''' Protected members are accessible only within the package, class, or its subclass 
    where it is define . The protected members starts with '_' ( underscore ) symbol
    followed by variable name .
     Whenever the variable is prefixedwith  '_' ( underscore ) automatically the
     accessibility of the variable will be restricted '''